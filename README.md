# Arzaquna Admin Dashboard

React-based admin dashboard for managing the Arzaquna mobile application.

## Features

- 📊 Dashboard with statistics
- 👥 User management
- 🏪 Vendor management and application review
- 📦 Product approval system
- 📁 Category management
- 🖼️ Slider/Banner management
- 🛒 Order management
- 💬 Message management
- 📄 App content management (About, Privacy, Terms)
- ⚙️ Settings

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start development server:
   ```bash
   npm run dev
   ```

3. Open http://localhost:3001

## Build

```bash
npm run build
```

The build output will be in the `dist/` directory.

## Environment

The dashboard connects to the backend API at `http://localhost:3000` by default (configured in `vite.config.js`).

Make sure the backend server is running before using the dashboard.



